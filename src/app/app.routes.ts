import { Routes } from '@angular/router';

export const routes: Routes = [
  {
    path: 'drag-drop',
    // loadChildren: () =>
    //   import('../features/drag-drop/drag-drop.routes').then(
    //     (m) => m.DRAG_DROP_ROUTES
    //   ),
  },
];
